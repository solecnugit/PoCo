export class segment{
    sequence_number: number;
    total_number: number;

    constructor(sequence: number, total: number){
        this.sequence_number = sequence;
        this.total_number = total;
    }
}